version: '3.8'
services:
  bot:
    build: .
    restart: always
    env_file: .env
    volumes:
      - ./src:/app/src    # Монтируем локальную директорию src
      - ./logs:/app/logs  # Логи сохраняем локально